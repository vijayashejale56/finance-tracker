# ═══════════════════════════════════════
# Base Configuration — no secrets here
# ═══════════════════════════════════════

# Active profile — override with env var
spring.profiles.active=${SPRING_PROFILE:dev}

# Server
server.port=${PORT:8080}

# JPA common settings
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# Logging
logging.level.com.financetracker=DEBUG
logging.level.org.springframework.security=INFO




spring.flyway.baseline-on-migrate=true
spring.flyway.out-of-order=true

# Actuator — health check endpoint
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never



# # Server
# server.port=8080

# # PostgreSQL
# spring.datasource.url=jdbc:postgresql://localhost:5432/financetracker
# spring.datasource.username=postgres
# spring.datasource.password=root
# spring.datasource.driver-class-name=org.postgresql.Driver

# # JPA
# spring.jpa.hibernate.ddl-auto=validate
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# # Flyway
# spring.flyway.enabled=true
# spring.flyway.locations=classpath:db/migration

# # JWT
# # app.jwt.secret=thisIsAVeryLongSecretKeyForJWTThatMustBeAtLeast256BitsLong1234567890
# # app.jwt.expiration-ms=900000
# app.jwt.secret=thisIsAVeryLongSecretKeyForFinanceTrackerJWTThatIsAtLeast64CharactersLong1234567890ABCDEF
# app.jwt.expiration-ms=86400000

# # Enable detailed logging for Spring Security to see why authentication fails
# logging.level.org.springframework.security=DEBUG

# # Show the actual values replacing the "?" in your Hibernate logs
# logging.level.org.hibernate.orm.results=TRACE
# logging.level.org.hibernate.stat=DEBUG

# # CORS
# app.cors.allowed-origins=http://localhost:5173